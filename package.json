{
  "name": "emoticore-td",
  "private": true,
  "version": "0.1.0-alpha",
  "type": "module",
  "description": "EMOTICORE TD — emotional tower defense in WebGL",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "deploy:server": "powershell -ExecutionPolicy Bypass -File tools/deploy-server.ps1",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "pixi-filters": "^6.0.5",
    "pixi.js": "^8.6.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
